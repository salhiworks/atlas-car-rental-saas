/**
 * Constants Vite inlines at build time.
 *
 * `__APP_VERSION__` is `package.json`'s version, defined in vite.config.ts so the
 * product has exactly one place that states which build it is.
 */
declare const __APP_VERSION__: string
